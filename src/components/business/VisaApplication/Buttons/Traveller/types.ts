export interface VisaTravellerStatusActionProps {
  applicationNo?: string;
  applicantNo?: string;
  currentStatus?: string;
  buttonType?: 'link' | 'primary' | 'text' | 'default' | 'dashed';
  buttonBlock?: boolean;
}
